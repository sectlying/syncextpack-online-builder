oldFolder = "\SyncExtendedPack\Apps\Nav\Navitel"
newFolder = "\SyncExtendedPack\Apps\Nav\Navitel_new"

If(DirExists(oldFolder))
  DelTree(oldFolder)
  RmDir(oldFolder)
  Rename(newFolder, oldFolder)
EndIf
