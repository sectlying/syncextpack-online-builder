oldFolder = "\SyncExtendedPack\Apps\Media\EasyConnected"
newFolder = "\SyncExtendedPack\Apps\Media\EasyConnected_new"

If(DirExists(oldFolder))
  DelTree(oldFolder)
  RmDir(oldFolder)
  Rename(newFolder, oldFolder)
EndIf
